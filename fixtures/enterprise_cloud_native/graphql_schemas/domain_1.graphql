# GraphQL Subgraph Schema Specification for Domain 1
extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key", "@shareable", "@provides", "@requires"])

type DomainEntity1 @key(fields: "id") {
  id: ID!
  name: String!
  slug: String!
  status: String!
  metricValue: Float!
  tags: [String!]!
  createdAt: String!
  updatedAt: String!
}

input CreateDomainEntity1Input {
  name: String!
  slug: String!
  metricValue: Float!
  tags: [String!] = []
}

extend type Query {
  getDomainEntity1(id: ID!): DomainEntity1
  listDomainEntities1(limit: Int = 20, offset: Int = 0): [DomainEntity1!]!
}

extend type Mutation {
  createDomainEntity1(input: CreateDomainEntity1Input!): DomainEntity1!
  deleteDomainEntity1(id: ID!): Boolean!
}
