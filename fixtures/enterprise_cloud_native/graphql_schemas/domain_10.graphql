# GraphQL Subgraph Schema Specification for Domain 10
extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key", "@shareable", "@provides", "@requires"])

type DomainEntity10 @key(fields: "id") {
  id: ID!
  name: String!
  slug: String!
  status: String!
  metricValue: Float!
  tags: [String!]!
  createdAt: String!
  updatedAt: String!
}

input CreateDomainEntity10Input {
  name: String!
  slug: String!
  metricValue: Float!
  tags: [String!] = []
}

extend type Query {
  getDomainEntity10(id: ID!): DomainEntity10
  listDomainEntities10(limit: Int = 20, offset: Int = 0): [DomainEntity10!]!
}

extend type Mutation {
  createDomainEntity10(input: CreateDomainEntity10Input!): DomainEntity10!
  deleteDomainEntity10(id: ID!): Boolean!
}
