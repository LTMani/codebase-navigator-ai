# GraphQL Subgraph Schema Specification for Domain 11
extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key", "@shareable", "@provides", "@requires"])

type DomainEntity11 @key(fields: "id") {
  id: ID!
  name: String!
  slug: String!
  status: String!
  metricValue: Float!
  tags: [String!]!
  createdAt: String!
  updatedAt: String!
}

input CreateDomainEntity11Input {
  name: String!
  slug: String!
  metricValue: Float!
  tags: [String!] = []
}

extend type Query {
  getDomainEntity11(id: ID!): DomainEntity11
  listDomainEntities11(limit: Int = 20, offset: Int = 0): [DomainEntity11!]!
}

extend type Mutation {
  createDomainEntity11(input: CreateDomainEntity11Input!): DomainEntity11!
  deleteDomainEntity11(id: ID!): Boolean!
}
