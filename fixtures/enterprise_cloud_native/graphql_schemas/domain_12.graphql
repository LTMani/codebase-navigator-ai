# GraphQL Subgraph Schema Specification for Domain 12
extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key", "@shareable", "@provides", "@requires"])

type DomainEntity12 @key(fields: "id") {
  id: ID!
  name: String!
  slug: String!
  status: String!
  metricValue: Float!
  tags: [String!]!
  createdAt: String!
  updatedAt: String!
}

input CreateDomainEntity12Input {
  name: String!
  slug: String!
  metricValue: Float!
  tags: [String!] = []
}

extend type Query {
  getDomainEntity12(id: ID!): DomainEntity12
  listDomainEntities12(limit: Int = 20, offset: Int = 0): [DomainEntity12!]!
}

extend type Mutation {
  createDomainEntity12(input: CreateDomainEntity12Input!): DomainEntity12!
  deleteDomainEntity12(id: ID!): Boolean!
}
