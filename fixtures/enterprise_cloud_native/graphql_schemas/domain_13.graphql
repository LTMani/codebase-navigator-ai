# GraphQL Subgraph Schema Specification for Domain 13
extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key", "@shareable", "@provides", "@requires"])

type DomainEntity13 @key(fields: "id") {
  id: ID!
  name: String!
  slug: String!
  status: String!
  metricValue: Float!
  tags: [String!]!
  createdAt: String!
  updatedAt: String!
}

input CreateDomainEntity13Input {
  name: String!
  slug: String!
  metricValue: Float!
  tags: [String!] = []
}

extend type Query {
  getDomainEntity13(id: ID!): DomainEntity13
  listDomainEntities13(limit: Int = 20, offset: Int = 0): [DomainEntity13!]!
}

extend type Mutation {
  createDomainEntity13(input: CreateDomainEntity13Input!): DomainEntity13!
  deleteDomainEntity13(id: ID!): Boolean!
}
