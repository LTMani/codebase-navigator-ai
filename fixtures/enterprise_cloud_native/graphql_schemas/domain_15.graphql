# GraphQL Subgraph Schema Specification for Domain 15
extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key", "@shareable", "@provides", "@requires"])

type DomainEntity15 @key(fields: "id") {
  id: ID!
  name: String!
  slug: String!
  status: String!
  metricValue: Float!
  tags: [String!]!
  createdAt: String!
  updatedAt: String!
}

input CreateDomainEntity15Input {
  name: String!
  slug: String!
  metricValue: Float!
  tags: [String!] = []
}

extend type Query {
  getDomainEntity15(id: ID!): DomainEntity15
  listDomainEntities15(limit: Int = 20, offset: Int = 0): [DomainEntity15!]!
}

extend type Mutation {
  createDomainEntity15(input: CreateDomainEntity15Input!): DomainEntity15!
  deleteDomainEntity15(id: ID!): Boolean!
}
