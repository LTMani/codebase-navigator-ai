# GraphQL Subgraph Schema Specification for Domain 18
extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key", "@shareable", "@provides", "@requires"])

type DomainEntity18 @key(fields: "id") {
  id: ID!
  name: String!
  slug: String!
  status: String!
  metricValue: Float!
  tags: [String!]!
  createdAt: String!
  updatedAt: String!
}

input CreateDomainEntity18Input {
  name: String!
  slug: String!
  metricValue: Float!
  tags: [String!] = []
}

extend type Query {
  getDomainEntity18(id: ID!): DomainEntity18
  listDomainEntities18(limit: Int = 20, offset: Int = 0): [DomainEntity18!]!
}

extend type Mutation {
  createDomainEntity18(input: CreateDomainEntity18Input!): DomainEntity18!
  deleteDomainEntity18(id: ID!): Boolean!
}
