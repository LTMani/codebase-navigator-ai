# GraphQL Subgraph Schema Specification for Domain 19
extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key", "@shareable", "@provides", "@requires"])

type DomainEntity19 @key(fields: "id") {
  id: ID!
  name: String!
  slug: String!
  status: String!
  metricValue: Float!
  tags: [String!]!
  createdAt: String!
  updatedAt: String!
}

input CreateDomainEntity19Input {
  name: String!
  slug: String!
  metricValue: Float!
  tags: [String!] = []
}

extend type Query {
  getDomainEntity19(id: ID!): DomainEntity19
  listDomainEntities19(limit: Int = 20, offset: Int = 0): [DomainEntity19!]!
}

extend type Mutation {
  createDomainEntity19(input: CreateDomainEntity19Input!): DomainEntity19!
  deleteDomainEntity19(id: ID!): Boolean!
}
