# GraphQL Subgraph Schema Specification for Domain 2
extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key", "@shareable", "@provides", "@requires"])

type DomainEntity2 @key(fields: "id") {
  id: ID!
  name: String!
  slug: String!
  status: String!
  metricValue: Float!
  tags: [String!]!
  createdAt: String!
  updatedAt: String!
}

input CreateDomainEntity2Input {
  name: String!
  slug: String!
  metricValue: Float!
  tags: [String!] = []
}

extend type Query {
  getDomainEntity2(id: ID!): DomainEntity2
  listDomainEntities2(limit: Int = 20, offset: Int = 0): [DomainEntity2!]!
}

extend type Mutation {
  createDomainEntity2(input: CreateDomainEntity2Input!): DomainEntity2!
  deleteDomainEntity2(id: ID!): Boolean!
}
