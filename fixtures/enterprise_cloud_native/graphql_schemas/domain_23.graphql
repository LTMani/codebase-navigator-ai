# GraphQL Subgraph Schema Specification for Domain 23
extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key", "@shareable", "@provides", "@requires"])

type DomainEntity23 @key(fields: "id") {
  id: ID!
  name: String!
  slug: String!
  status: String!
  metricValue: Float!
  tags: [String!]!
  createdAt: String!
  updatedAt: String!
}

input CreateDomainEntity23Input {
  name: String!
  slug: String!
  metricValue: Float!
  tags: [String!] = []
}

extend type Query {
  getDomainEntity23(id: ID!): DomainEntity23
  listDomainEntities23(limit: Int = 20, offset: Int = 0): [DomainEntity23!]!
}

extend type Mutation {
  createDomainEntity23(input: CreateDomainEntity23Input!): DomainEntity23!
  deleteDomainEntity23(id: ID!): Boolean!
}
