# GraphQL Subgraph Schema Specification for Domain 28
extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key", "@shareable", "@provides", "@requires"])

type DomainEntity28 @key(fields: "id") {
  id: ID!
  name: String!
  slug: String!
  status: String!
  metricValue: Float!
  tags: [String!]!
  createdAt: String!
  updatedAt: String!
}

input CreateDomainEntity28Input {
  name: String!
  slug: String!
  metricValue: Float!
  tags: [String!] = []
}

extend type Query {
  getDomainEntity28(id: ID!): DomainEntity28
  listDomainEntities28(limit: Int = 20, offset: Int = 0): [DomainEntity28!]!
}

extend type Mutation {
  createDomainEntity28(input: CreateDomainEntity28Input!): DomainEntity28!
  deleteDomainEntity28(id: ID!): Boolean!
}
