# GraphQL Subgraph Schema Specification for Domain 6
extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key", "@shareable", "@provides", "@requires"])

type DomainEntity6 @key(fields: "id") {
  id: ID!
  name: String!
  slug: String!
  status: String!
  metricValue: Float!
  tags: [String!]!
  createdAt: String!
  updatedAt: String!
}

input CreateDomainEntity6Input {
  name: String!
  slug: String!
  metricValue: Float!
  tags: [String!] = []
}

extend type Query {
  getDomainEntity6(id: ID!): DomainEntity6
  listDomainEntities6(limit: Int = 20, offset: Int = 0): [DomainEntity6!]!
}

extend type Mutation {
  createDomainEntity6(input: CreateDomainEntity6Input!): DomainEntity6!
  deleteDomainEntity6(id: ID!): Boolean!
}
