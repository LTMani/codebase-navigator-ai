# GraphQL Subgraph Schema Specification for Domain 7
extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key", "@shareable", "@provides", "@requires"])

type DomainEntity7 @key(fields: "id") {
  id: ID!
  name: String!
  slug: String!
  status: String!
  metricValue: Float!
  tags: [String!]!
  createdAt: String!
  updatedAt: String!
}

input CreateDomainEntity7Input {
  name: String!
  slug: String!
  metricValue: Float!
  tags: [String!] = []
}

extend type Query {
  getDomainEntity7(id: ID!): DomainEntity7
  listDomainEntities7(limit: Int = 20, offset: Int = 0): [DomainEntity7!]!
}

extend type Mutation {
  createDomainEntity7(input: CreateDomainEntity7Input!): DomainEntity7!
  deleteDomainEntity7(id: ID!): Boolean!
}
