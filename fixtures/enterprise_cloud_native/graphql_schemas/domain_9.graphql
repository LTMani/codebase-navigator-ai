# GraphQL Subgraph Schema Specification for Domain 9
extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key", "@shareable", "@provides", "@requires"])

type DomainEntity9 @key(fields: "id") {
  id: ID!
  name: String!
  slug: String!
  status: String!
  metricValue: Float!
  tags: [String!]!
  createdAt: String!
  updatedAt: String!
}

input CreateDomainEntity9Input {
  name: String!
  slug: String!
  metricValue: Float!
  tags: [String!] = []
}

extend type Query {
  getDomainEntity9(id: ID!): DomainEntity9
  listDomainEntities9(limit: Int = 20, offset: Int = 0): [DomainEntity9!]!
}

extend type Mutation {
  createDomainEntity9(input: CreateDomainEntity9Input!): DomainEntity9!
  deleteDomainEntity9(id: ID!): Boolean!
}
