# Kubernetes Microservice Deployment Manifest 11
apiVersion: apps/v1
kind: Deployment
metadata:
  name: enterprise-service-11
  labels:
    app: enterprise-service-11
    tier: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: enterprise-service-11
  template:
    metadata:
      labels:
        app: enterprise-service-11
    spec:
      containers:
      - name: enterprise-service-11
        image: navigator/enterprise-service-11:2.4.0
        ports:
        - containerPort: 8080
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "100m"
            memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: enterprise-service-11-svc
spec:
  selector:
    app: enterprise-service-11
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
