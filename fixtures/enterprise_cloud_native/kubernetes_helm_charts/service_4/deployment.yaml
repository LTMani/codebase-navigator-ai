# Kubernetes Microservice Deployment Manifest 4
apiVersion: apps/v1
kind: Deployment
metadata:
  name: enterprise-service-4
  labels:
    app: enterprise-service-4
    tier: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: enterprise-service-4
  template:
    metadata:
      labels:
        app: enterprise-service-4
    spec:
      containers:
      - name: enterprise-service-4
        image: navigator/enterprise-service-4:2.4.0
        ports:
        - containerPort: 8080
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "100m"
            memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: enterprise-service-4-svc
spec:
  selector:
    app: enterprise-service-4
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
