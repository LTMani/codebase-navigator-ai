# OpenAPI 3.1.0 Enterprise Specification for API Gateway Route 1
openapi: 3.1.0
info:
  title: Enterprise Microservice API 1
  version: 2.5.0
  description: High-throughput enterprise REST interface for service domain 1
paths:
  /api/v1/domain-1/resources:
    get:
      summary: List all resources in domain 1
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Successful query response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ResourceItem1"
    post:
      summary: Create resource in domain 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResourceItem1"
      responses:
        "201":
          description: Resource created successfully
components:
  schemas:
    ResourceItem1:
      type: object
      required:
        - id
        - name
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        score:
          type: number
        isActive:
          type: boolean
